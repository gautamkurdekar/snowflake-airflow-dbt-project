version: 2

models:
  - name: fct_orders
    description: "Fact table for customer orders"
    columns:
      - name: order_id
        tests:
          - not_null
          - unique

      - name: customer_id
        tests:
          - not_null
